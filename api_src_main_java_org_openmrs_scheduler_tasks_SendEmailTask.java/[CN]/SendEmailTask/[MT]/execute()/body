{
  try {
    Context.openSession();
    log.info("****************************** SEND EMAIL TASK:  Executing task ...");
    if (!Context.isAuthenticated()) {
      authenticate();
    }
  }
  finally {
    Context.closeSession();
  }
}
