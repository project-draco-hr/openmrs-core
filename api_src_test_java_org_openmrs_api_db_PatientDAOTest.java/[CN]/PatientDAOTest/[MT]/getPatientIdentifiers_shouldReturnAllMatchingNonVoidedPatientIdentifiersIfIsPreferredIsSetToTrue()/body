{
  List<PatientIdentifier> patientIdentifiers=dao.getPatientIdentifiers(null,new ArrayList<PatientIdentifierType>(),new ArrayList<Location>(),new ArrayList<Patient>(),Boolean.TRUE);
  Assert.assertEquals(2,patientIdentifiers.size());
}
