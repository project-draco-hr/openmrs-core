{
  if (!context.hasPrivilege(OpenmrsConstants.PRIV_EDIT_USERS))   throw new APIAuthenticationException("Privilege required: " + OpenmrsConstants.PRIV_EDIT_USERS);
  checkPrivileges(user);
  getUserDAO().updateUser(user);
}
