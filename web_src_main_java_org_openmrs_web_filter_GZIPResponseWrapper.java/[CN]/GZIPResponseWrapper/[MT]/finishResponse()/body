{
  try {
    if (writer != null) {
      writer.close();
    }
 else {
      if (stream != null) {
        stream.close();
      }
    }
  }
 catch (  IOException e) {
    log.error("Error during closing writer or stream",e);
  }
}
