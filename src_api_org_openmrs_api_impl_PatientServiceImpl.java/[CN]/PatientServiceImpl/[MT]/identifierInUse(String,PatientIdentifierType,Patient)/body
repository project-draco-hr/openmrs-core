{
  List<PatientIdentifier> ids=getPatientIdentifiers(identifier,type);
  for (  PatientIdentifier id : ids) {
    if (ignorePatient == null || !id.getPatient().equals(ignorePatient))     return id.getPatient();
  }
  return null;
}
