{
  boolean retVal=false;
  if (isObsAvailable) {
    double val=obsObj.getValueNumeric();
    if (val >= RHS) {
      retVal=true;
    }
  }
 else {
    retVal=false;
  }
  evaluated=retVal;
  isEvaluated=true;
  return retVal;
}
