{
  String retVal="";
  if (readType != null) {
    if (!readType.equals("")) {
      retVal="." + readType + "(";
      if (howMany > 0) {
        retVal+=howMany;
      }
      retVal+=")";
    }
  }
 else {
    retVal=".last()";
  }
  return retVal;
}
