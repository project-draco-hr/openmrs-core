{
  Assert.assertTrue(StringUtils.isBlank(Context.getAuthenticatedUser().getUserProperty(OpenmrsConstants.USER_PROPERTY_DEFAULT_LOCATION)));
  Assert.assertEquals("Unknown Location",Context.getLocation().getName());
}
