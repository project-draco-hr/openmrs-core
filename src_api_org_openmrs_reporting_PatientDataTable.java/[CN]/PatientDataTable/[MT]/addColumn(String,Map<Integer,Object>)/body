{
  if (!columnNames.contains(columnName))   columnNames.add(columnName);
  for (  Map.Entry<Integer,Object> e : values.entrySet()) {
    TableRow row=get(e.getKey());
    if (row != null)     row.put(columnName,e.getValue());
  }
}
