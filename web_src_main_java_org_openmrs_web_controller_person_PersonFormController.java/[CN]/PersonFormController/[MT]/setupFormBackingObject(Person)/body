{
  if (person.getNames().size() < 1)   person.addName(new PersonName());
  if (person.getAddresses().size() < 1)   person.addAddress(new PersonAddress());
  person.getAttributes().size();
  return person;
}
