{
  if (obj instanceof Person) {
    Person person=(Person)obj;
    if (getPersonId() == null || person.getPersonId() == null)     return super.equals(person);
    return (personId.equals(person.getPersonId()));
  }
  return false;
}
