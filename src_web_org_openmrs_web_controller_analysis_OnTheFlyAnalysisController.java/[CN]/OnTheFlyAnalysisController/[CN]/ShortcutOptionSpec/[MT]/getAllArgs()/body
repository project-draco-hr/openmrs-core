{
  List<String> ret=new ArrayList<String>();
  if (args != null) {
    for (    ShortcutArg arg : args)     if (!arg.isLabel())     ret.add(arg.getName());
  }
  if (hiddenArgNames != null) {
    ret.addAll(hiddenArgNames);
  }
  return ret;
}
