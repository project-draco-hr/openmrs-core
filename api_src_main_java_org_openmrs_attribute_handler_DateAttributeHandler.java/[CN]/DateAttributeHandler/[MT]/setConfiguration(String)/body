{
  Map<String,String> config=AttributeUtil.deserializeSimpleConfiguration(configString);
  if (config.containsKey("allowFutureDates"))   allowFutureDates=Boolean.valueOf(config.get("allowFutureDates"));
}
