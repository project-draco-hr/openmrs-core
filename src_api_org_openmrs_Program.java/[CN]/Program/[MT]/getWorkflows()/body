{
  Set<ProgramWorkflow> ret=new HashSet<ProgramWorkflow>();
  if (this.workflows != null) {
    for (    ProgramWorkflow workflow : this.workflows) {
      if (workflow.isRetired() == false)       ret.add(workflow);
    }
  }
  return ret;
}
