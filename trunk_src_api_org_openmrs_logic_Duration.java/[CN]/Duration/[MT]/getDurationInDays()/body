{
switch (units) {
case SECONDS:
    return duration / 86400;
case MINUTES:
  return duration / 1440;
case DAYS:
return duration;
case WEEKS:
return duration * 7;
case YEARS:
return duration * 365;
default :
return 0d;
}
}
