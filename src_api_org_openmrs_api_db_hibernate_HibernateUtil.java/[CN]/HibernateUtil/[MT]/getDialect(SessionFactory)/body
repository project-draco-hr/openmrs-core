{
  SessionFactoryImplementor implementor=(SessionFactoryImplementor)sessionFactory;
  Dialect dialect=implementor.getDialect();
  if (log.isDebugEnabled())   log.debug("Getting dialect for session: " + dialect);
  return dialect;
}
