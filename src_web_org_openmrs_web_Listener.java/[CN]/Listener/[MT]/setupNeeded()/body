{
  if (!runtimePropertiesFound)   return true;
  try {
    return DatabaseUpdater.updatesRequired() && !DatabaseUpdater.allowAutoUpdate();
  }
 catch (  Throwable t) {
    throw new ServletException(t);
  }
}
