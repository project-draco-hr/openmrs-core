{
  if (program.getWorkflows() == null)   return null;
  for (  ProgramWorkflow wf : program.getWorkflows())   if (wf.getConcept().getName(Context.getUserContext().getLocale(),false).getName().equals(name))   return wf;
  return null;
}
