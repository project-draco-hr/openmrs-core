{
  if (obj instanceof Patient) {
    Patient p=(Patient)obj;
    if (this.getPatientId() != null && p.getPatientId() != null)     return (this.getPatientId().equals(p.getPatientId()));
  }
  return false;
}
