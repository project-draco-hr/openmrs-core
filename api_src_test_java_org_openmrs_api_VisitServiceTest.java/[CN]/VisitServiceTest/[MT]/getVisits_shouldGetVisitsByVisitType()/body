{
  List<VisitType> visitTypes=new ArrayList<VisitType>();
  visitTypes.add(new VisitType(1));
  assertEquals(4,Context.getVisitService().getVisits(visitTypes,null,null,null,null,null,null,null,null,true,false).size());
}
