{
  List<Concept> indications=new ArrayList<Concept>();
  indications.add(new Concept(5497));
  Assert.assertEquals(1,Context.getVisitService().getVisits(null,null,null,indications,null,null,null,null,false).size());
}
