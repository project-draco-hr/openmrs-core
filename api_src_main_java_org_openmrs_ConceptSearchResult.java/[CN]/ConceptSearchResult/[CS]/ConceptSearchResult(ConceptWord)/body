{
  if (conceptWord != null) {
    this.concept=conceptWord.getConcept();
    this.conceptName=conceptWord.getConceptName();
    this.word=conceptWord.getWord();
    if (conceptWord.getWeight() != null)     this.weight=conceptWord.getWeight();
  }
}
