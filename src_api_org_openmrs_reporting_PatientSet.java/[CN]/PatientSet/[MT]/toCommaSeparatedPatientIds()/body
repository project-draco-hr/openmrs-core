{
  StringBuilder sb=new StringBuilder();
  for (Iterator<Integer> i=patientIds.iterator(); i.hasNext(); ) {
    sb.append(i.next());
    if (i.hasNext()) {
      sb.append(",");
    }
  }
  return sb.toString();
}
