{
  if (getAttributes() == null)   setAttributes(new HashSet<AttrType>());
  for (  AttrType existing : getAttributes())   if (existing.getAttributeType().equals(attribute.getAttributeType()))   existing.setVoided(true);
  getAttributes().add(attribute);
  attribute.setOwner(this);
}
