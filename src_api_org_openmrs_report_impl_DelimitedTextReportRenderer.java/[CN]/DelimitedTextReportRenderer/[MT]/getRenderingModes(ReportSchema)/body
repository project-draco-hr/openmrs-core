{
  if (schema.getDataSetDefinitions() == null || schema.getDataSetDefinitions().size() != 1)   return null;
 else   return Collections.singleton(new RenderingMode(this,this.getLabel(),null,Integer.MIN_VALUE));
}
