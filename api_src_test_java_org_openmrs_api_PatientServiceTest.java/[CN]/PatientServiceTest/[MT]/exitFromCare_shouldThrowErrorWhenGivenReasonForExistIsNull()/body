{
  try {
    patientService.exitFromCare(patientService.getPatient(7),new Date(),Context.getConceptService().getConcept(16));
  }
 catch (  Exception e) {
    fail("failed with correct parameters");
  }
  patientService.exitFromCare(patientService.getPatient(8),new Date(),null);
}
