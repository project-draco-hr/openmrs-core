{
  initializeInMemoryDatabase();
  executeDataSet(FIND_PATIENTS_XML);
  Context.getAdministrationService().saveGlobalProperty(new GlobalProperty(OpenmrsConstants.GLOBAL_PROPERTY_MIN_SEARCH_CHARACTERS,"2"));
  assertEquals(1,Context.getPatientService().getPatients("Ho").size());
}
