{
  if (obj == null || !(obj instanceof LogicCacheEntryKey))   return false;
  LogicCacheEntryKey cek=(LogicCacheEntryKey)obj;
  if (cek.type != type)   return false;
  if (!safeEquals(cek.criteria,criteria))   return false;
  if (type == LogicCacheEntryType.RULE) {
    if (!safeEquals(cek.parameters,parameters))     return false;
    return true;
  }
 else   if (type == LogicCacheEntryType.DATA_ELEMENT) {
    if (!safeEquals(cek.dataSource,dataSource))     return false;
    return true;
  }
  return false;
}
