{
  if (locale != null && getNames(locale).size() > 0) {
    for (    ConceptName conceptName : getNames(locale)) {
      if (ObjectUtils.nullSafeEquals(conceptName.isFullySpecifiedName(),true))       return conceptName;
    }
  }
  return null;
}
