{
  if (patientIdentifierType.getPatientIdentifierTypeId() == null)   createPatientIdentifierType(patientIdentifierType);
 else   sessionFactory.getCurrentSession().saveOrUpdate(patientIdentifierType);
}
