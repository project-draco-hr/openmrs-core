{
  if (filename.indexOf("/") != -1) {
    filename=filename.substring(filename.lastIndexOf("/"));
  }
  if (filename.indexOf("\\") != -1) {
    filename=filename.substring(filename.lastIndexOf("\\"));
  }
  return filename;
}
