{
  if (order.getOrderId() == null)   createOrder(order);
 else {
    log.debug("Updating existing order: " + order.getOrderId().toString());
    sessionFactory.getCurrentSession().saveOrUpdate(order);
  }
}
