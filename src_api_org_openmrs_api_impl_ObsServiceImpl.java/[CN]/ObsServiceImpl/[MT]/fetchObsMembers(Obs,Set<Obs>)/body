{
  if (parentObs.isObsGrouping()) {
    for (    Obs obsInner : parentObs.getGroupMembers()) {
      if (childMembers.add(obsInner) == true)       fetchObsMembers(obsInner,childMembers);
    }
  }
}
