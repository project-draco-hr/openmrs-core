{
  executeDataSet(PROVIDERS_INITIAL_XML);
  if (dao == null)   dao=(HibernateConceptDAO)applicationContext.getBean("conceptDAO");
  sessionFactory=(SessionFactory)applicationContext.getBean("sessionFactory");
}
