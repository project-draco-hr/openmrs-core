{
  String val=p.getProperty("scheduler.username",null);
  if (val != null)   SchedulerConstants.SCHEDULER_USERNAME=val;
  val=p.getProperty("scheduler.password",null);
  if (val != null)   SchedulerConstants.SCHEDULER_PASSWORD=val;
  startup();
}
