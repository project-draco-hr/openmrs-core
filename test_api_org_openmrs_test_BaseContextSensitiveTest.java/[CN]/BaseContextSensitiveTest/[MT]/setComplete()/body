{
  if (useInMemoryDatabase() == true)   throw new UnsupportedOperationException("Completing a transaction for an in-memory database does not make sense. You should override useInMemoryDatabase() to return false");
  super.setComplete();
}
