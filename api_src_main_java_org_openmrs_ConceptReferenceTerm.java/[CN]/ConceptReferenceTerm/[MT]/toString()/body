{
  if (getCode() != null && getName() != null)   return getName() + "(" + getCode()+ ")";
 else   if (getCode() == null)   return getName();
 else   if (getName() == null)   return getCode();
  return "";
}
