{
  StringBuilder ret=new StringBuilder();
  for (  Object o : list) {
    if (ret.length() > 0)     ret.append(" ");
    if (o instanceof List)     ret.append("(" + nameHelper((List)o) + ")");
 else     ret.append(o);
  }
  return ret.toString();
}
