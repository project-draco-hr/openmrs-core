{
  if (obj instanceof ConceptSet) {
    if (concept == null || conceptSet == null)     return false;
    ConceptSet c=(ConceptSet)obj;
    return (this.concept.equals(c.getConcept()) && this.conceptSet.equals(c.getConceptSet()));
  }
  return false;
}
