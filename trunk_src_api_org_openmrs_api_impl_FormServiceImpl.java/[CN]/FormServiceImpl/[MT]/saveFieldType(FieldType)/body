{
  if (fieldType.getCreator() == null)   fieldType.setCreator(Context.getAuthenticatedUser());
  if (fieldType.getDateCreated() == null)   fieldType.setDateCreated(new Date());
  return dao.saveFieldType(fieldType);
}
