{
  if (aaData != null && aaData.get(0).length != sColumns.length) {
    throw new IllegalArgumentException("Must contain " + aaData.get(0).length + " columns whereas "+ sColumns.length+ " given");
  }
  this.sColumns=sColumns;
}
