{
  List<FormFieldListItem> formFields=new Vector<FormFieldListItem>();
  Form form=Context.getFormService().getForm(formId);
  for (  FormField ff : form.getFormFields())   formFields.add(new FormFieldListItem(ff,Context.getLocale()));
  return formFields;
}
