{
  if (!context.hasPrivilege(OpenmrsConstants.PRIV_DELETE_ORDERS))   throw new APIAuthenticationException("Privilege required: " + OpenmrsConstants.PRIV_DELETE_ORDERS);
  getOrderDAO().deleteOrder(order);
}
