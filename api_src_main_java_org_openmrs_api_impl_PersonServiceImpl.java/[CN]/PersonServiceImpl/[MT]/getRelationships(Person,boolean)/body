{
  if (showVoided)   throw new APIException("Voided relationships should be considered gone and unusable.  Don't search for or show them");
  return Context.getPersonService().getRelationshipsByPerson(p);
}
