{
  List<ConceptWord> conceptWords=getConceptWords(phrase,locales,includeRetired,requireClasses,excludeClasses,requireDatatypes,excludeDatatypes,answersToConcept,start,size);
  return createSearchResultsList(conceptWords);
}
