{
  if (interpretJstl) {
    if (log.isDebugEnabled())     log.debug("returning last modified date of : " + lastModified + " for : "+ request.getPathInfo());
    return lastModified;
  }
  return -1;
}
