{
  PatientIdentifierType ret=(PatientIdentifierType)sessionFactory.getCurrentSession().createQuery("from PatientIdentifierType t where t.name = :name").setString("name",name).uniqueResult();
  return ret;
}
