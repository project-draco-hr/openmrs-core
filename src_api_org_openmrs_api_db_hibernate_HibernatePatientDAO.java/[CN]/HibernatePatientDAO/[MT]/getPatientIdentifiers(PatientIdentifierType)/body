{
  List<PatientIdentifier> patientIdentifiers=sessionFactory.getCurrentSession().createQuery("from PatientIdentifier p where p.identifierType = :pit and p.voided = false").setParameter("pit",pit).list();
  return patientIdentifiers;
}
