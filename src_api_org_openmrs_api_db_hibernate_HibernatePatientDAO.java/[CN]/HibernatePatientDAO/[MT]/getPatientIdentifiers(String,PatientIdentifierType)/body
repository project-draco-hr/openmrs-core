{
  List<PatientIdentifier> ids;
  ids=sessionFactory.getCurrentSession().createQuery("from PatientIdentifier p where p.identifierType = :pit and p.identifier = :id and p.voided = false").setParameter("pit",pit).setString("id",identifier).list();
  return ids;
}
