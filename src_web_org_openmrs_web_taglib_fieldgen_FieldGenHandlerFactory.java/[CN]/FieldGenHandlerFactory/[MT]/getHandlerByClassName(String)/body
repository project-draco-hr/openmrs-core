{
  if (className != null) {
    if (singleton.handlers.containsKey(className)) {
      return singleton.handlers.get(className);
    }
 else {
      return null;
    }
  }
 else {
    return null;
  }
}
