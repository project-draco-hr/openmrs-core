{
  List<Concept> answers=new Vector<Concept>();
  answers.add(answer);
  return Context.getObsService().getObservations(null,null,null,answers,getPersonTypeEnumerations(personType),null,null,null,null,null,null,includeVoided);
}
