{
  conceptName.setConcept(this);
  if (getNames() == null)   names=new HashSet<ConceptName>();
  if (conceptName != null && !names.contains(conceptName)) {
    names.add(conceptName);
    if (compatibleCache != null) {
      compatibleCache.clear();
    }
  }
}
