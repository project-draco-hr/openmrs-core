{
  newConceptMap.setConcept(this);
  if (conceptMappings == null)   conceptMappings=new HashSet<ConceptMap>();
  if (newConceptMap != null && !conceptMappings.contains(newConceptMap))   conceptMappings.add(newConceptMap);
}
