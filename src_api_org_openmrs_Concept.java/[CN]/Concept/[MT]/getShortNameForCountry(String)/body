{
  if (!StringUtils.isBlank(country)) {
    for (    ConceptName shortName : getShortNames()) {
      if (shortName.getLocale() != null && shortName.getLocale().getCountry().equals(country))       return shortName;
    }
  }
  return null;
}
