{
  String label="";
  if (formField.getFieldNumber() != null)   label=formField.getFieldNumber() + ".";
  if (formField.getFieldPart() != null)   label+=formField.getFieldPart();
  if (label.equals(""))   return "--";
 else   return label;
}
