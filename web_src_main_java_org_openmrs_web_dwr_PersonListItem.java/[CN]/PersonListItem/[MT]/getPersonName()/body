{
  String name="";
  if (StringUtils.hasText(givenName))   name=givenName;
  if (StringUtils.hasText(middleName))   name=name + (name.length() > 0 ? " " : "") + middleName;
  if (StringUtils.hasText(familyName))   name=name + (name.length() > 0 ? " " : "") + familyName;
  return name;
}
