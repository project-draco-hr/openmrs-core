{
  if (orderType.getCreator() == null)   orderType.setCreator(Context.getAuthenticatedUser());
  if (orderType.getDateCreated() == null)   orderType.setDateCreated(new Date());
  return dao.saveOrderType(orderType);
}
