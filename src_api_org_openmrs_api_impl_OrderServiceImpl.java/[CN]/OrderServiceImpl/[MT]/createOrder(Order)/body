{
  if (!Context.hasPrivilege(OpenmrsConstants.PRIV_MANAGE_ORDERS))   throw new APIAuthenticationException("Privilege required: " + OpenmrsConstants.PRIV_MANAGE_ORDERS);
  getOrderDAO().createOrder(order);
}
