{
  checkIfLocked();
  String authUserId=Context.getAuthenticatedUser().getUserId().toString();
  log.info("creating drug - " + authUserId + "|"+ drug.getConcept().toString());
  getConceptDAO().createDrug(drug);
}
