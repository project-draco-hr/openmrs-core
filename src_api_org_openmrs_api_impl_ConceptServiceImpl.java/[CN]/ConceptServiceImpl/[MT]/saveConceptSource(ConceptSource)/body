{
  if (conceptSource.getCreator() == null)   conceptSource.setCreator(Context.getAuthenticatedUser());
  if (conceptSource.getDateCreated() == null)   conceptSource.setDateCreated(new Date());
  return dao.saveConceptSource(conceptSource);
}
