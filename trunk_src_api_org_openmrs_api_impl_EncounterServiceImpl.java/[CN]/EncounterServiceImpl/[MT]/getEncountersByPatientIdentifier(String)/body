{
  List<Encounter> encs=new Vector<Encounter>();
  for (  Patient p : Context.getPatientService().getPatients(null,identifier,null)) {
    encs.addAll(getEncountersByPatientId(p.getPatientId()));
  }
  return encs;
}
