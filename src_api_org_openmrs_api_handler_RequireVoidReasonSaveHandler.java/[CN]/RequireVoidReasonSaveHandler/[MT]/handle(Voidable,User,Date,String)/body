{
  if (voidableObject.isVoided() && StringUtils.isBlank(voidableObject.getVoidReason())) {
    throw new APIException("The voided bit was set to true, so a void reason is required at save time for object: " + voidableObject + " of class: "+ voidableObject.getClass());
  }
}
