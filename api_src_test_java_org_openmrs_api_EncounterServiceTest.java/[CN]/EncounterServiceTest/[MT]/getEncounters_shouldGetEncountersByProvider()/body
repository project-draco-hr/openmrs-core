{
  List<User> providers=new ArrayList<User>();
  providers.add(new User(1));
  List<Encounter> encounters=Context.getEncounterService().getEncounters(null,null,null,null,null,null,providers,true);
  assertEquals(4,encounters.size());
}
