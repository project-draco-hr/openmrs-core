{
  if (concept.getConceptId() == null || concept.getConceptId() < 1)   concept.setConceptId(this.getNextAvailableId());
  sessionFactory.getCurrentSession().saveOrUpdate(concept);
  return concept;
}
