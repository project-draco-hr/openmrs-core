{
  if (StringUtils.isBlank(persistedValue))   return null;
  try {
    return new SimpleDateFormat(dateFormat).parse(persistedValue);
  }
 catch (  Exception ex) {
    throw new InvalidCustomValueException("Invalid date: " + persistedValue);
  }
}
