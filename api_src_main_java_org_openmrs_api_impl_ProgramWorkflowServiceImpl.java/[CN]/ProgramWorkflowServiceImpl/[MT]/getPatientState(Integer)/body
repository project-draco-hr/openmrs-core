{
  for (  PatientProgram p : Context.getProgramWorkflowService().getPatientPrograms(null,null,null,null,null,null,false)) {
    PatientState state=p.getPatientState(patientStateId);
    if (state != null) {
      return state;
    }
  }
  return null;
}
