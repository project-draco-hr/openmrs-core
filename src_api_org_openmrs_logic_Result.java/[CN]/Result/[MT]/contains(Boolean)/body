{
  if (valueList != null) {
    for (    Result r : valueList) {
      if (r.contains(valueBoolean))       return true;
    }
    return false;
  }
  if (this.valueBoolean == null)   return false;
  return (this.valueBoolean.equals(valueBoolean));
}
