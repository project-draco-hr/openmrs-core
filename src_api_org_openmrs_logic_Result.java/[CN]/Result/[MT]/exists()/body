{
  if (valueList != null) {
    if (valueList.size() < 1)     return false;
    for (    Result r : valueList) {
      if (r.exists())       return true;
    }
    return false;
  }
  return !isNull();
}
