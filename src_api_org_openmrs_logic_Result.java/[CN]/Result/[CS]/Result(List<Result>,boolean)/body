{
  if (resultList == null || resultList.size() < 1)   init(NULL_RESULT);
 else   if (resultList.size() < 2)   init(resultList.get(0));
 else {
    valueList=new Vector<Result>();
    for (    Result r : resultList)     valueList.add(r);
  }
}
