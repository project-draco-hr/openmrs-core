{
  if (obj instanceof Person) {
    Person person=(Person)obj;
    if (getPersonId() != null && person.getPersonId() != null)     return personId.equals(person.getPersonId());
  }
  return this == obj;
}
