{
  String retVal="";
  if (StringUtils.isNotEmpty(readType) && !readType.equalsIgnoreCase("exist")) {
    retVal="." + readType;
  }
 else {
    retVal=".last";
  }
  if (howMany > 1) {
    retVal+="(" + howMany + ")";
  }
 else {
    retVal+="()";
  }
  return retVal;
}
