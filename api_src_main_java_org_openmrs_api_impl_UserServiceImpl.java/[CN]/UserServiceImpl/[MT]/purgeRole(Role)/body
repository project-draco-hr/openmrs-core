{
  if (role == null || role.getRole() == null)   return;
  if (OpenmrsUtil.getCoreRoles().keySet().contains(role.getRole()))   throw new APIException("Cannot delete a core role");
  dao.deleteRole(role);
}
