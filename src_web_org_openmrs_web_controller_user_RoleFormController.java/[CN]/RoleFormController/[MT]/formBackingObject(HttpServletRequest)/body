{
  Role role=null;
  if (Context.isAuthenticated()) {
    UserService us=Context.getUserService();
    String r=request.getParameter("role");
    if (r != null)     role=us.getRole(r);
  }
  if (role == null)   role=new Role();
  return role;
}
