{
  if (reportObj.getReportObjectId() == null)   createReportObject(reportObj);
 else {
    ReportObjectWrapper wrappedReportObject=new ReportObjectWrapper(reportObj);
    sessionFactory.getCurrentSession().update(wrappedReportObject);
  }
}
