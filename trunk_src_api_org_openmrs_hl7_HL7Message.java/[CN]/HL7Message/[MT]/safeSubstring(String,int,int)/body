{
  if (beginIndex < 0 || beginIndex >= s.length() || endIndex <= beginIndex)   return "";
  if (endIndex >= s.length())   return s.substring(beginIndex);
  return s.substring(beginIndex,endIndex);
}
