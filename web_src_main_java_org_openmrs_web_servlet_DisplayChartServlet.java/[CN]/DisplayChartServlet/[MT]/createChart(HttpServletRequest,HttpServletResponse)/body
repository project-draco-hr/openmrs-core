{
  String key=request.getParameter(CHART_KEY);
  HttpSession session=request.getSession();
  Object o=session.getAttribute(key);
  if (o == null)   log.error("Unable to find chart in session with key: " + key);
  return (JFreeChart)o;
}
