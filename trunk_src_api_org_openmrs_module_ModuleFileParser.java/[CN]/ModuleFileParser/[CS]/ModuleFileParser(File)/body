{
  if (moduleFile == null)   throw new ModuleException("Module file cannot be null");
  if (!moduleFile.getName().endsWith(".omod"))   throw new ModuleException("Module file does not have the correct .omod file extension",moduleFile.getName());
  this.moduleFile=moduleFile;
}
