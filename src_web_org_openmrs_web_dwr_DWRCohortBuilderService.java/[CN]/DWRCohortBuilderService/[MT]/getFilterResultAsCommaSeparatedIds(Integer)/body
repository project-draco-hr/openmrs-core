{
  PatientFilter pf=Context.getReportObjectService().getPatientFilterById(filterId);
  if (pf == null)   return "";
 else   return Context.getPatientSetService().getAllPatients().getCommaSeparatedPatientIds();
}
