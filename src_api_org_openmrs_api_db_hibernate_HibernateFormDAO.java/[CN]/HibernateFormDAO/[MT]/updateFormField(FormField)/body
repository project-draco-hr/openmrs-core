{
  if (formField.getFormFieldId() == null)   createFormField(formField);
 else   sessionFactory.getCurrentSession().merge(formField);
  log.debug("formField cache mode: " + sessionFactory.getCurrentSession().getCacheMode());
}
