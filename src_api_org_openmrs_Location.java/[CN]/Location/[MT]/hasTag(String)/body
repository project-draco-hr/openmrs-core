{
  if (tagToFind != null && getTags() != null) {
    for (    LocationTag locTag : getTags()) {
      if (locTag.getTag().equals(tagToFind)) {
        return true;
      }
    }
  }
  return false;
}
