{
  StringBuilder sb=new StringBuilder();
  sb.append("<textarea id=\"" + widgetId + "\" rows=\""+ rows+ "\" cols=\""+ cols+ "\" name=\""+ formFieldName+ "\">");
  if (startingValue != null)   sb.append(WebUtil.escapeHTML(startingValue));
  sb.append("</textarea>");
  return sb.toString();
}
