{
  if (getConcept() == null)   return null;
  ConceptName conceptName=getConceptName();
  if (conceptName == null || concept == null)   return null;
  return concept.getDescription(conceptName.getLocale());
}
