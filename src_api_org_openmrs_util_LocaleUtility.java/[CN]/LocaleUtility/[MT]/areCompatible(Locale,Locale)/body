{
  if (lhs.equals(rhs)) {
    return true;
  }
 else   if ((lhs.getCountry() == "") || (rhs.getCountry() == "")) {
    if (lhs.getLanguage().equals(rhs.getLanguage())) {
      return true;
    }
  }
  return false;
}
