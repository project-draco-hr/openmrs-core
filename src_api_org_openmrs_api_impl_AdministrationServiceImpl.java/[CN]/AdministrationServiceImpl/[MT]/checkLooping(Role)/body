{
  log.debug("allParentRoles: " + role.getAllParentRoles());
  log.debug("role: " + role);
  if (role.getAllParentRoles().contains(role))   throw new APIAuthenticationException("Invalid Role or parent Role.  A role cannot inherit itself.");
}
