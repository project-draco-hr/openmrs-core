{
  if (!Context.hasPrivilege(OpenmrsConstants.PRIV_MANAGE_MIME_TYPES))   throw new APIAuthenticationException("Privilege required: " + OpenmrsConstants.PRIV_MANAGE_MIME_TYPES);
  getAdministrationDAO().updateMimeType(mimeType);
}
