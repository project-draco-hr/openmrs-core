{
  StringBuffer sb=new StringBuffer();
  if (object instanceof Map)   toJSONString((Map<String,Object>)object,sb);
 else   if (object instanceof List)   toJSONString((List)object,sb);
 else   if (object instanceof Boolean)   sb.append(object.toString());
 else   toJSONString(object,sb);
  return sb.toString();
}
