{
  boolean first=true;
  sb.append('[');
  for (  Object listItem : list) {
    if (first)     first=false;
 else     sb.append(',');
    sb.append(toJSONString(listItem,escapeJavascript));
  }
  sb.append(']');
}
