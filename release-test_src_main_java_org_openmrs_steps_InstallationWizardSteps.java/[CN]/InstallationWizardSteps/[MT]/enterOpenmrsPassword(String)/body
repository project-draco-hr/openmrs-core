{
  type(password,into(passwordtextbox().with(attribute("name",equalTo("new_admin_password")))));
  type(password,into(passwordtextbox().with(attribute("name",equalTo("new_admin_password_confirm")))));
}
