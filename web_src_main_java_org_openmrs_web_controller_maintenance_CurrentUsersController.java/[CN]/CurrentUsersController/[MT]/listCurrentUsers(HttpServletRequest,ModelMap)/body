{
  log.debug("List current users");
  if (!Context.hasPrivilege(PrivilegeConstants.VIEW_USERS))   throw new APIAuthenticationException("Privilege required: " + PrivilegeConstants.VIEW_USERS);
  modelMap.put("currentUsers",CurrentUsers.getCurrentUsernames(request.getSession()));
}
