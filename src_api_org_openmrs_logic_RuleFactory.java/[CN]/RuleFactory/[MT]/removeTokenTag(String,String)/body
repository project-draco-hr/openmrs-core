{
  if (tagsByToken.containsKey(token) && tagsByToken.get(token).contains(tag))   tagsByToken.get(token).remove(tag);
  if (tokensByTag.containsKey(tag) && tokensByTag.get(tag).contains(token))   tokensByTag.get(tag).remove(token);
}
