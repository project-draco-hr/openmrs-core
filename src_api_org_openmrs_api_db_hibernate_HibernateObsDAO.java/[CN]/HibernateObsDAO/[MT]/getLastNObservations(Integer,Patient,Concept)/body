{
  Session session=HibernateUtil.currentSession();
  Query query=session.createQuery("from Obs obs where obs.patient = :p and obs.concept = :c order by obs.obsDatetime desc");
  query.setParameter("p",who);
  query.setParameter("c",question);
  query.setFetchSize(n);
  return query.list();
}
