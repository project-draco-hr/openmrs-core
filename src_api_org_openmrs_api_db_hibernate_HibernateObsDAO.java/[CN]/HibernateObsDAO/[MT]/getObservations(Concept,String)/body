{
  Session session=HibernateUtil.currentSession();
  Query query=session.createQuery("from Obs obs where obs.concept = :c and obs.voided = false order by " + sort).setParameter("c",question);
  return query.list();
}
