{
  Session session=HibernateUtil.currentSession();
  Criteria crit=session.createCriteria(Encounter.class).add(Expression.between("encounterDatetime",fromDate,toDate)).addOrder(Order.asc("location")).addOrder(Order.asc("encounterDatetime"));
  return crit.list();
}
