{
  Criteria crit=sessionFactory.getCurrentSession().createCriteria(Encounter.class).add(Expression.between("encounterDatetime",fromDate,toDate)).add(Expression.eq("voided",false)).addOrder(Order.asc("location")).addOrder(Order.asc("encounterDatetime"));
  return crit.list();
}
