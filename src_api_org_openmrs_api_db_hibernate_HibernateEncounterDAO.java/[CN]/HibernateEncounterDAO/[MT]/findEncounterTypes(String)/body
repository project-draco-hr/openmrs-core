{
  return sessionFactory.getCurrentSession().createCriteria(EncounterType.class).add(Expression.ilike("name",name,MatchMode.START)).addOrder(Order.asc("name")).addOrder(Order.asc("retired")).list();
}
