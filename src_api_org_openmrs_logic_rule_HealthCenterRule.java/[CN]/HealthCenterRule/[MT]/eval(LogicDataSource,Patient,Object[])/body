{
  Location healthCenter=null;
  if (healthCenter == null)   return new Result("UNKNOWN");
  Result result=new Result(healthCenter.getName());
  result.setValueNumeric(healthCenter.getLocationId());
  return result;
}
