{
  if (schema.getDataSetDefinitions().size() == 1 && schema.getDataSetDefinitions().get(0) instanceof CohortDataSetDefinition) {
    return Collections.singleton(new RenderingMode(this,this.getLabel(),null,100));
  }
 else {
    return null;
  }
}
