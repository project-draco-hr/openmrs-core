{
  if (encounterType.getCreator() == null)   encounterType.setCreator(Context.getAuthenticatedUser());
  if (encounterType.getDateCreated() == null)   encounterType.setDateCreated(new Date());
  dao.saveEncounterType(encounterType);
  return encounterType;
}
