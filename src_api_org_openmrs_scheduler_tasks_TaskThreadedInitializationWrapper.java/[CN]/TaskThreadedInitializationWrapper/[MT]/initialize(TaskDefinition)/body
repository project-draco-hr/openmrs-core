{
  Runnable r=new Runnable(){
    public void run(){
      lock.lock();
      task.initialize(config);
      initialized=true;
      initializedCond.signalAll();
      lock.unlock();
    }
  }
;
  new Thread(r).start();
}
