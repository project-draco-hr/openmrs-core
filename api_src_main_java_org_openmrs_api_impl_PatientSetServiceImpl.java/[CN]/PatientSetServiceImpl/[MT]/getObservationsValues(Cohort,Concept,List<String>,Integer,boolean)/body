{
  if (attributes == null)   attributes=new Vector<String>();
  if (attributes.size() < 1 || attributes.get(0) != null)   attributes.add(0,null);
  return getPatientSetDAO().getObservationsValues(patients,c,attributes,limit,showMostRecentFirst);
}
