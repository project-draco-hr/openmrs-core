{
  Collection<Privilege> privileges=role.getPrivileges();
  for (  Privilege p : privileges) {
    if (!context.hasPrivilege(p.getPrivilege()))     throw new APIAuthenticationException("Privilege required: " + p);
  }
}
