{
  if (!context.hasPrivilege(OpenmrsConstants.PRIV_MANAGE_LOCATIONS))   throw new APIAuthenticationException("Privilege required: " + OpenmrsConstants.PRIV_MANAGE_LOCATIONS);
  getAdminDAO().updateLocation(location);
}
