{
  if (!context.hasPrivilege(OpenmrsConstants.PRIV_MANAGE_FIELD_TYPES))   throw new APIAuthenticationException("Privilege required: " + OpenmrsConstants.PRIV_MANAGE_FIELD_TYPES);
  getAdminDAO().deleteFieldType(fieldType);
}
