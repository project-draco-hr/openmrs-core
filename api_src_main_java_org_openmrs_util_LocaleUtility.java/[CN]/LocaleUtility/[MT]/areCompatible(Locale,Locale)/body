{
  if (lhs.equals(rhs)) {
    return true;
  }
 else   if (("".equals(lhs.getCountry())) || ("".equals(rhs.getCountry()))) {
    if (lhs.getLanguage().equals(rhs.getLanguage())) {
      return true;
    }
  }
  return false;
}
