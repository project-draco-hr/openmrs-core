{
  try {
    return (LocaleUtils.isAvailableLocale(new Locale(locale.getLanguage())) || locale.getISO3Language() != null) && locale.getISO3Country() != null;
  }
 catch (  MissingResourceException e) {
    return false;
  }
}
