{
  String[] mlmNames={"leadspecpws2.mlm"};
  String defaultPath="test/arden test/";
  startup();
  Context.authenticate("vibha","chicachica");
  for (int i=0; i < mlmNames.length; i++) {
    Context.getArdenService().compileFile(defaultPath + mlmNames[i]);
  }
  shutdown();
}
