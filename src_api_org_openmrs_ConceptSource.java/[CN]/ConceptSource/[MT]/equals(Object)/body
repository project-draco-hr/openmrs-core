{
  if (obj instanceof ConceptSource) {
    if (conceptSourceId == null)     return false;
    ConceptSource c=(ConceptSource)obj;
    return (conceptSourceId.equals(c.getConceptSourceId()));
  }
  return false;
}
